<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="@dimen/genre_card_size"
    android:layout_height="@dimen/genre_card_size"
    android:padding="10dp"
    android:background="@drawable/shape_genre_card_background">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text="Example Genre"
        android:layout_marginTop="25dp"
        android:id="@+id/genre_card_title"
        android:gravity="center"
        android:layout_centerHorizontal="true"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="25dp"
        android:id="@+id/genre_card_color"
        android:src="@drawable/shape_genre_card_color"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>